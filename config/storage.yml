test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
amazon:
   service: S3
   access_key_id: <%= ENV["AWS_ACCESS_KEY"] %>
   secret_access_key: <%= ENV["AWS_ACCESS_SECRET"] %>
   region: us-east-1
   bucket: cloudsys-group11
   #bucket: cloudsys-group11-<%= Rails.env %>

exoscale:
  service: S3
  endpoint: https://ch-gva-2.exo.io
  region: ch-gva-2
  access_key_id: EXO2b25fe435a990a0ec6df8fbd # TODO: teo
  secret_access_key: sOjkB-vahx9mZXfo_vOkfi1LO8hA1akeu4hN0MyEYQc # TODO: teo
  bucket: pw1-s3

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# Use bin/rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
microsoft:
  service: AzureStorage
  storage_account_name: azures3cloud # TODO: teo
  storage_access_key: isWIUBR/Q0hLOnMH/NWbfTVoTIMvunC1+dtm/l0fcY5oGgpyH84WvJd5GodKQcugttKZVtLGBEuF+AStRgxAkA== # TODO: teo
  container: cloud

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
